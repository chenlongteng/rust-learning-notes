大家好，我给大家介绍一些常见特征，
了解这些特征会让大家写的Rust程序更接近标准库的风格。
首先Drop特征，最近读Rust圣经时肯定有涉及到，Rust是无GC的，
取而代之的是所有权，当一个值的拥有者消失时，rust就会丢弃该值。
大部分情况，rust都会自动处理丢弃值的工作。
那如果想自定义丢弃当前值的操作，就要实现Drop特征。
读文字。

第二个Sized特征，这是一个标记类特征，它指的是